HOW TO RUN:
	Set the directory to the folder where the app folder exists. If the app         exists in parent_folder/InteractivPlots, set directory to parent_folder 
	> setwd("parent_folder/")
	> install.packages("shiny")
	> library("shiny")
	Run this app by using runApp function
	> runApp("InteractivePlots")